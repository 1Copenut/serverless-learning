{
  "name": "@continuumdesign/router-worker",
  "description": "A Cloudflare worker with more in-depth routing and returning different data types based on endpoint",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.7",
    "@cloudflare/workers-types": "^4.20251115.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "4.47.0"
  }
}
