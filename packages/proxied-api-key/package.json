{
  "name": "@continuumdesign/serverless-api",
  "description": "A first project using Cloudflare Workers to hit an Unsplash API endpoint with a proxied API key",
  "version": "0.0.0",
  "devDependencies": {
    "wrangler": "3.28.2"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  }
}
